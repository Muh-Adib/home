[opcache]
; Enable OPcache
opcache.enable = 1

; OPcache shared memory storage size (in MB)
opcache.memory_consumption = 256

; The amount of memory for internal strings (in MB)
opcache.interned_strings_buffer = 16

; The maximum number of keys (scripts) in the OPcache hash table
opcache.max_accelerated_files = 10000

; The maximum percentage of "wasted" memory until a restart is scheduled
opcache.max_wasted_percentage = 5

; When disabled, changes to scripts are not checked
opcache.validate_timestamps = 0

; How often (in seconds) to check file timestamps for changes to the shared memory storage allocation
opcache.revalidate_freq = 0

; Enables or disables file search in include_path optimization
opcache.revalidate_path = 0

; If disabled, all OPcache errors are silently ignored
opcache.log_verbosity_level = 1

; All OPcache errors go to the Web server log
opcache.report_meminfo = 0

; Allows exclusion of large files from being cached
opcache.max_file_size = 0

; Check the cache checksum each N requests
opcache.consistency_checks = 0

; How long to wait (in seconds) for a scheduled restart to begin if the cache is not being accessed
opcache.force_restart_timeout = 180

; OPcache error_log file name. Empty string assumes "stderr"
opcache.error_log = /var/log/opcache_errors.log

; All OPcache errors go to the Web server log
opcache.log_verbosity_level = 1

; Enables or disables copying of PHP code (text segment) into HUGE PAGES
opcache.huge_code_pages = 1

; Validate cached file permissions
opcache.validate_permission = 0

; Prevent name collisions in chroot'ed environment
opcache.validate_root = 0

; If enabled, OPcache appends the current working directory to the script key
opcache.use_cwd = 1

; Enables and sets the second level cache directory
; opcache.file_cache = /tmp

; Enables or disables opcode caching in shared memory
opcache.file_cache_only = 0

; Enables or disables checksum validation when script loaded from file cache
opcache.file_cache_consistency_checks = 1

; Implies opcache.file_cache_only=1 for a certain process that failed to reattach to the shared memory
opcache.file_cache_fallback = 1

; Enables or disables copying of comments to the opcode array
opcache.save_comments = 1

; If enabled, compilation warnings will be recorded and replayed each time a file is included
opcache.record_warnings = 0

; Allow file existence override (file_exists, is_file, etc.) performance feature
opcache.enable_file_override = 0

; A bitmask, where each bit enables or disables the appropriate OPcache passes
opcache.optimization_level = 0x7FFFBFFF

; The location of the OPcache blacklist file
; opcache.blacklist_filename = /path/to/blacklist.txt

; Allows exclusion of large files from being cached
opcache.max_file_size = 0 